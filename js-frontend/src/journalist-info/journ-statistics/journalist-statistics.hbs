<div class="statistics">
    {{#if data.articlesQuantity}}
        <h3>Статей (всего): 
            <span class="articles-count">
                {{data.articlesQuantity}}
            </span>
        </h3>
            <table class="table table-striped journ-statistics">
                <caption>
                    По изданиям
                </caption>
                <tbody>
                    {{#each data.articlesData}}
                    <tr>
                        <td>
                            {{title}}
                        </td>
                        <td>
                            {{count}}
                        </td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
            <table class="table table-striped journ-statistics">
                <caption>
                    По рубрикам
                </caption>
                <tbody>
                    {{#each data.topicsData}}
                    <tr>
                        <td>
                            {{title}}
                        </td>
                        <td>
                            {{count}}
                        </td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
    {{else}}
        <p class='alert alert-danger'>
            Пока что, у вас нет опубликованных статей.
        </p>
    {{/if}}
</div>
<style>
    .articles-count {
        font-weight: bold;
    }
    .statistics table td {
        width: 50%;
    }
    .statistics caption {
        font-size: 30px;
    }
    .journ-statistics td:nth-child(2)  {
        font-weight: bold;
    }
</style>